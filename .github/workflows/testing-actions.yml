name: Testing cookiecutter

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - python-version: [3.6]
            TOXENV: 'py36'
          - python-version: [3.7]
            TOXENV: 'py37, docs, flake8'
          - python-version: [3.8]
            TOXENV: 'py38, docs, flake8'

    steps:
      - uses: actions/checkout@v2
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python }}
      - name: Install Tox
        run: |
          python -m pip install --upgrade pip
          pip install tox
      - name: Run Tox
        run: tox
